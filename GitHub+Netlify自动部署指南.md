# GitHub + Netlify 自动部署完整指南

## 🎯 目标

实现：修改代码 → 推送到 GitHub → Netlify 自动检测 → 自动构建 → 自动部署 → 网站自动更新

**完全自动化，无需手动上传！**

---

## 📋 完整设置步骤

### 第一步：安装 Git（如果还没有）

1. 下载 Git：https://git-scm.com/download/win
2. 安装时选择默认选项
3. 安装完成后重启终端

---

### 第二步：初始化 Git 仓库

在项目根目录打开终端，执行：

```bash
# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit"
```

---

### 第三步：创建 GitHub 仓库

1. **访问 GitHub**
   - 访问：https://github.com
   - 登录你的账户（如果没有，先注册）

2. **创建新仓库**
   - 点击右上角 "+" → "New repository"
   - 输入仓库名称（如：`blockchain-storage`）
   - 选择 **Public**（公开）或 **Private**（私有）
   - **不要**勾选 "Initialize this repository with a README"
   - 点击 "Create repository"

---

### 第四步：推送代码到 GitHub

```bash
# 添加远程仓库（替换 YOUR_USERNAME 和 REPO_NAME）
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 设置主分支
git branch -M main

# 推送到 GitHub
git push -u origin main
```

**如果提示需要认证：**

GitHub 现在需要使用访问令牌（Personal Access Token）而不是密码：

1. **创建访问令牌**
   - 登录 GitHub
   - 点击右上角头像 → **Settings**
   - 左侧菜单 → **Developer settings**
   - **Personal access tokens** → **Tokens (classic)**
   - 点击 **"Generate new token"** → **"Generate new token (classic)"**
   - 输入 Token 名称（如：`Netlify Deploy`）
   - 选择权限：勾选 `repo`（完整仓库访问权限）
   - 点击 **"Generate token"**
   - **复制 token**（只显示一次，务必保存！）

2. **使用 token 推送**
   ```bash
   # 推送时，用户名输入你的 GitHub 用户名
   # 密码输入刚才复制的 token（不是登录密码）
   git push -u origin main
   ```

---

### 第五步：在 Netlify 连接 GitHub

1. **登录 Netlify**
   - 访问：https://app.netlify.com
   - 登录你的账户（如果没有，先注册）

2. **添加新站点**
   - 点击右上角 **"Add new site"**
   - 选择 **"Import an existing project"**

3. **选择 GitHub**
   - 在 Git 提供商列表中选择 **"GitHub"**
   - 点击 **"Authorize Netlify"** 授权访问

4. **授权 Netlify**
   - 会跳转到 GitHub 授权页面
   - 确认授权信息
   - 点击 **"Authorize Netlify"** 授权

5. **选择仓库**
   - 在仓库列表中找到你的项目
   - 点击选择

6. **配置构建设置**
   - **Branch to deploy（部署分支）**：`main`
   - **Build command（构建命令）**：`npm run build`
   - **Publish directory（发布目录）**：`dist`
   - **Base directory（基础目录）**：留空或填写 `.`

7. **部署站点**
   - 点击 **"Deploy site"**
   - 等待构建完成（通常 1-3 分钟）

---

### 第六步：完成！测试自动部署

#### 测试步骤：

1. **修改本地代码**
   - 例如：修改 `views/HomeView.vue` 中的文字

2. **提交并推送**
   ```bash
   git add .
   git commit -m "测试自动部署"
   git push origin main
   ```

3. **观察 Netlify**
   - 登录 Netlify 控制台
   - 进入你的站点
   - 点击 **"Deploys"** 标签页
   - 你会看到新的部署自动开始
   - 等待 1-2 分钟，部署完成

4. **验证更新**
   - 访问你的网站
   - 刷新页面（可能需要强制刷新：Ctrl+F5）
   - 应该能看到刚才的更改

**如果看到更新，说明自动部署成功！** 🎉

---

## 🔄 日常开发流程

以后每次修改代码后，只需：

```bash
# 1. 修改代码

# 2. 提交更改
git add .
git commit -m "更新功能描述"

# 3. 推送到 GitHub
git push origin main

# 4. Netlify 自动检测并部署
# 等待 1-2 分钟，网站自动更新！
```

---

## 📝 配置文件说明

项目已包含 `netlify.toml` 配置文件，Netlify 会自动读取：

```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

这个配置确保：
- ✅ 构建命令正确
- ✅ 发布目录正确
- ✅ 单页应用路由正常工作

---

## 🎨 自定义域名（可选）

### 修改站点名称（获得更短的链接）

1. 在 Netlify 控制台，进入你的站点
2. 点击 **"Site settings"**（站点设置）
3. 在 **"General"** 标签页
4. 找到 **"Site details"** → **"Change site name"**
5. 输入简短名称（如：`blockchain-storage`）
6. 保存后，链接变成：`https://blockchain-storage.netlify.app`

### 添加自定义域名（如果有自己的域名）

1. 在站点设置 → **"Domain settings"**
2. 点击 **"Add custom domain"**
3. 输入你的域名
4. 按照提示配置 DNS 记录
5. 等待 DNS 生效（通常几分钟到几小时）

---

## ❓ 常见问题

### Q: 推送代码时提示需要认证？
**A:** GitHub 需要使用访问令牌（Personal Access Token）：
1. GitHub → Settings → Developer settings → Personal access tokens
2. 创建新 token，选择 `repo` 权限
3. 推送时，密码输入 token（不是登录密码）

### Q: 推送代码后，Netlify 没有自动部署？
**A:** 检查：
1. Netlify 是否已连接 GitHub 仓库
2. 是否推送到 `main` 分支
3. Netlify 设置 → Build & deploy → Continuous Deployment 是否启用

### Q: 构建失败怎么办？
**A:** 
1. 查看 Netlify 的构建日志
2. 确保 `package.json` 中有 `build` 脚本
3. 检查依赖是否正确安装
4. 确认 `netlify.toml` 配置正确

### Q: 如何回滚到之前的版本？
**A:** 
1. 登录 Netlify 控制台
2. 进入 "Deploys" 标签页
3. 找到之前的部署
4. 点击 "Publish deploy"

### Q: 如何查看部署历史？
**A:** 在 Netlify 控制台 → 你的站点 → "Deploys" 标签页

---

## ✅ 检查清单

完成以下检查，确保一切正常：

- [ ] Git 已安装并配置
- [ ] 代码已推送到 GitHub（`git push origin main` 成功）
- [ ] 在 Netlify 中已连接 GitHub 仓库
- [ ] 构建设置正确（Build command: `npm run build`, Publish: `dist`）
- [ ] 首次部署成功
- [ ] 测试自动部署功能（修改代码 → 推送 → 观察 Netlify 自动部署）

---

## 🚀 完成后的效果

设置完成后，你的工作流程将是：

1. **开发**：在本地修改代码
2. **测试**：运行 `npm run dev` 本地测试
3. **提交**：`git add .` → `git commit -m "描述"`
4. **推送**：`git push origin main`
5. **自动部署**：Netlify 自动检测 → 自动构建 → 自动部署
6. **完成**：1-2 分钟后网站自动更新

**完全自动化，无需手动上传！** 🎉

---

## 📚 更多资源

- [GitHub 官方文档](https://docs.github.com/)
- [Netlify 官方文档](https://docs.netlify.com/)
- [Git 入门教程](https://git-scm.com/book/zh/v2)

---

## 🎯 快速开始（5分钟设置）

1. **初始化 Git**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **创建 GitHub 仓库并推送**
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
   git branch -M main
   git push -u origin main
   ```

3. **在 Netlify 连接 GitHub**
   - 登录 Netlify
   - Add new site → Import from Git → GitHub
   - 选择仓库
   - 配置：Build command: `npm run build`, Publish: `dist`
   - Deploy

4. **完成！** 以后只需 `git push`，网站自动更新！

---

**提示**：如果遇到问题，查看上面的常见问题部分，或告诉我具体的错误信息。

