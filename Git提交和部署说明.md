# Git 提交和部署说明

## ✅ 当前状态

代码已成功提交并推送到 GitHub 远程仓库：
- **提交ID**: `5daeebb`
- **提交信息**: "Optimize page layout and color scheme"
- **修改文件**: 9个文件，958行新增，72行删除
- **远程仓库**: `git@github.com:qinghai-x/blockchain-storage.git`

## 📋 日常提交和推送步骤

### 方法一：使用命令行（推荐）

1. **查看更改状态**
   ```powershell
   git status
   ```

2. **添加所有更改的文件**
   ```powershell
   git add .
   ```
   或者添加特定文件：
   ```powershell
   git add App.vue views/LoginView.vue
   ```

3. **提交更改**
   ```powershell
   git commit -m "描述你的更改内容"
   ```
   例如：
   ```powershell
   git commit -m "优化登录页面样式"
   git commit -m "修复文件上传bug"
   git commit -m "添加新功能"
   ```

4. **推送到远程仓库**
   ```powershell
   git push origin main
   ```

### 方法二：使用批处理脚本（快速）

创建一个 `提交并推送.bat` 文件：

```batch
@echo off
chcp 65001 >nul
echo 正在添加所有更改...
git add .
echo.
echo 请输入提交信息：
set /p commit_msg="> "
echo.
echo 正在提交更改...
git commit -m "%commit_msg%"
echo.
echo 正在推送到远程仓库...
git push origin main
echo.
echo 完成！
pause
```

然后双击运行即可。

## 🚀 Netlify 自动部署

### 前提条件

确保 Netlify 已经连接到你的 GitHub 仓库：

1. 登录 [Netlify](https://app.netlify.com)
2. 进入你的站点设置
3. 检查 **Build & deploy** → **Continuous Deployment**
4. 确认连接的是 `qinghai-x/blockchain-storage` 仓库
5. 确认分支是 `main`

### 自动部署流程

当你执行 `git push origin main` 后：

1. **GitHub 接收推送** (几秒钟)
   - 代码推送到 GitHub 远程仓库

2. **Netlify 检测到更改** (1-2分钟)
   - Netlify 自动检测到 `main` 分支有新提交
   - 开始构建部署

3. **Netlify 构建项目** (2-5分钟)
   - 运行构建命令（通常是 `npm run build`）
   - 生成 `dist` 文件夹

4. **Netlify 部署网站** (1-2分钟)
   - 将构建结果部署到 CDN
   - 网站自动更新

**总计时间**: 约 5-10 分钟

### 查看部署状态

1. 访问 [Netlify Dashboard](https://app.netlify.com)
2. 点击你的站点
3. 在 **Deploys** 标签页查看部署状态
4. 绿色 ✅ = 部署成功
5. 红色 ❌ = 部署失败（点击查看错误日志）

### 部署通知

Netlify 会在以下情况发送通知：
- ✅ 部署成功
- ❌ 部署失败
- ⚠️ 构建警告

## 🔍 常见问题

### 1. 推送失败：权限被拒绝

**错误信息**：
```
Permission denied (publickey)
```

**解决方法**：
- 使用 HTTPS 方式推送（需要 Personal Access Token）
- 或者配置 SSH 密钥

**切换到 HTTPS**：
```powershell
git remote set-url origin https://github.com/qinghai-x/blockchain-storage.git
```

然后推送时需要输入：
- 用户名：`qinghai-x`
- 密码：使用 GitHub Personal Access Token（不是账户密码）

### 2. 推送失败：需要先拉取

**错误信息**：
```
! [rejected] main -> main (fetch first)
```

**解决方法**：
```powershell
git pull origin main
# 解决可能的冲突后
git push origin main
```

### 3. Netlify 没有自动部署

**检查清单**：
- ✅ GitHub 仓库连接正常
- ✅ 分支名称是 `main`（不是 `master`）
- ✅ Netlify 构建设置正确
- ✅ 构建命令：`npm run build`
- ✅ 发布目录：`dist`

**手动触发部署**：
1. 进入 Netlify Dashboard
2. 点击 **Trigger deploy** → **Deploy site**

### 4. 构建失败

**常见原因**：
- 代码语法错误
- 依赖包缺失
- 环境变量未配置

**查看日志**：
1. 进入 Netlify Dashboard
2. 点击失败的部署
3. 查看 **Deploy log** 了解详细错误

## 📝 提交信息规范

建议使用清晰的提交信息：

**格式**：
```
类型: 简短描述

详细说明（可选）
```

**类型示例**：
- `feat`: 新功能
- `fix`: 修复bug
- `style`: 样式调整
- `refactor`: 代码重构
- `docs`: 文档更新
- `chore`: 构建/工具相关

**示例**：
```powershell
git commit -m "feat: 添加文件批量上传功能"
git commit -m "fix: 修复登录页面样式问题"
git commit -m "style: 优化页面布局和颜色方案"
git commit -m "refactor: 重构用户认证逻辑"
```

## 🎯 快速命令参考

```powershell
# 查看状态
git status

# 添加所有更改
git add .

# 提交
git commit -m "你的提交信息"

# 推送
git push origin main

# 查看提交历史
git log --oneline -10

# 查看远程仓库
git remote -v
```

## ⚡ 一键提交脚本

创建 `快速提交.bat`：

```batch
@echo off
chcp 65001 >nul
title Git 快速提交
color 0A

echo ========================================
echo    Git 快速提交工具
echo ========================================
echo.

echo [1/4] 检查更改状态...
git status --short
echo.

echo [2/4] 添加所有更改...
git add .
echo ✓ 已添加所有更改
echo.

echo [3/4] 提交更改...
set /p msg="请输入提交信息: "
git commit -m "%msg%"
echo ✓ 已提交
echo.

echo [4/4] 推送到远程仓库...
git push origin main
echo.

echo ========================================
echo    完成！Netlify 将自动部署
echo ========================================
echo.
echo 预计 5-10 分钟后网站将更新
echo.
pause
```

## 📞 需要帮助？

如果遇到问题：
1. 检查 Git 和 GitHub 连接
2. 查看 Netlify 部署日志
3. 确认构建配置正确

---

**最后更新**: 2025-01-XX
**仓库地址**: https://github.com/qinghai-x/blockchain-storage

